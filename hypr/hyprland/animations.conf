animations {
    enabled = yes

    bezier = easeOutQuint, 0.23, 1, 0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear, 0, 0, 1, 1
    bezier = almostLinear, 0.5, 0.5, 0.75, 1
    bezier = quick, 0.15, 0, 0.1, 1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint

    # windows
    animation = windows, 1, 4.79, easeOutQuint, slide
    animation = windowsIn, 1, 4.1, easeOutQuint, slide bottom
    animation = windowsOut, 1, 2.49, easeOutQuint, slide bottom

    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick

    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear

    # workspaces
    animation = workspaces, 1, 7, easeOutQuint, slide
    animation = zoomFactor, 1, 7, quick

    # Layers
    animation = layers, 1, 3.81, easeOutQuint, fade
    animation = layersIn, 1, 4, easeOutQuint, popin 80%
    animation = layersOut, 1, 1.5, easeInOutCubic, popin 80%
}

# LAYER RULES
layerrule = blur on, match:namespace waybar
layerrule = dim_around on, match:namespace rofi
layerrule = animation slide top , match:namespace waybar
layerrule = animation popin 80% , match:namespace rofi
